<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.management.mapper.PaperDao">

    <select id="findByPage" resultType="Paper">
    select * from paper
    limit #{start},#{rows}
   </select>

    <insert id="save">
       insert into paper (score,time,bankId) values (#{score},#{time},#{bankId})
    </insert>

    <delete id="delete">
        delete  from paper where id=#{id}
    </delete>

    <update id="update">
     update paper set score=#{score},time=#{time},bankId=#{bankId} where id=#{id}
    </update>

    <select id="findById">
     select  * from paper where id=#{id}
    </select>

    <select id="findTotals" resultType="Integer">
     select count(id) from paper
    </select>

    <select id="findOne" resultType="Paper">
        select * from paper where id=#{id}
    </select>


</mapper>